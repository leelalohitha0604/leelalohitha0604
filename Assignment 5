#include <stdio.h>

int main() {
    int arr[5],n,i,large;
    printf("enter the size of array");
    scanf("%d\n",&n);
    for(i=0;i<n;i++)
    scanf("%d",&arr[i]);
    large=arr[0];
    for(i=1;i<n;i++)
    if(arr[i]>large)
    large=arr[i];
    printf("large:%d ",large);
    return 0;
}
