#include <stdio.h>

int main() {
    int arr[5],n,i,small;
    printf("enter the size of array");
    scanf("%d\n",&n);
    for(i=0;i<n;i++)
    scanf("%d",&arr[i]);
    small=arr[0];
    for(i=1;i<n;i++)
    if(arr[i]<small)
    small=arr[i];
    printf("small:%d ",small);
    return 0;
}
