#include <stdio.h>

int main() {
   int n, i, x = 0;
  printf("Enter any number n:");
  scanf("%d", &n);

  for (i = 1; i <= n; i++) {
      if (n % i == 0) {
         x++;
      }
  }

  if (c == 2) {
        printf("%d is a Prime number");
  }
  else {
         printf("%d is not a Prime number");
  }
  return 0;    
}
